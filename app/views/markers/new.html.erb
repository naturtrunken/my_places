<h1><%= I18n.t('title', :scope => [:markers, :views, :new]) %></h1>

<div id="page_navigation">
  <div class="left">
    <%=
        link_to(
                I18n.t('back', :scope => [:general]),
                markers_path
        )
    %>
  </div>
  <div style="clear: both;"></div>
</div>

<%= render 'form' %>
